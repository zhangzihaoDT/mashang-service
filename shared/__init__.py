# shared — mashang-service shared layer
# Sub-packages:
#   schema/       — data schema, metric registry, business definitions
#   operators/   — deterministic business operators (lock/assign/atp/…)
#   loaders/     — dataset loaders (passenger_insurance, …)
