# Legacy Runtime Operators

This directory is retained for legacy runtime compatibility.

The canonical shared version now lives in:
- `shared/operators`

Do not add new shared business logic here.
Prefer `shared/operators` for shared primitives.
