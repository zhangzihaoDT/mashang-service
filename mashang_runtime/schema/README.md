# Legacy Runtime Schema

This directory is retained for legacy runtime compatibility.

The canonical shared version now lives in:
- `shared/schema`

Do not add new shared configuration here.
Prefer `shared/schema` for shared definitions.
